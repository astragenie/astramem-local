# AstraMemory Local

Local-first memory daemon for AI coding agents. Captures conversation transcripts, distills typed memories via an 8-stage pipeline, and serves hybrid search (FTS5 + vector). Wire-compatible with `memory-plugin` via a single environment-variable swap.
